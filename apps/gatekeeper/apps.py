from django.apps import AppConfig

class GatekeeperConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.gatekeeper'
    verbose_name = 'Gatekeeper Security & Visitor Desk'
